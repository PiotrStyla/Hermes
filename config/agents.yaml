# Agent Configurations
# Override models, temperature, and behavior per agent

researcher:
  # Model override (uses DEFAULT_MODEL from .env if not set)
  # model: "google/gemini-2.5-pro"
  temperature: 0.3
  max_tokens: 4096
  description: "Research and information gathering specialist"

writer:
  # model: "anthropic/claude-opus-4.6"
  temperature: 0.7
  max_tokens: 8192
  description: "Content creation and structuring specialist"

reviewer:
  # model: "anthropic/claude-sonnet-4"
  temperature: 0.2
  max_tokens: 4096
  description: "Quality assurance and feedback specialist"
