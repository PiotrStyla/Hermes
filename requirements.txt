openai>=1.50.0
pyyaml>=6.0
python-dotenv>=1.0.0
httpx>=0.27.0
rich>=13.0.0

# Voice mode (optional — only required when running with --voice)
elevenlabs>=1.10.0
sounddevice>=0.5.0
numpy>=1.26.0

# Scheduling (Phase 4 — only required for `scheduler start` / `schedule *` commands)
apscheduler>=3.10.0

# Dashboard (Phase 4 — only required for `dashboard` command)
jinja2>=3.1.0

# Telephony (Phase 3 — only required for `twilio-*` commands)
twilio>=9.3.0
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.9  # FastAPI form parsing (Twilio webhooks send form-encoded)
